"У репозиторії проєкту має бути файл README, де коротко і структуровано описано:
назва проєкту;
про проєкт і яку задачу він вирішує;
які технології використані;
як запустити/розгорнути проєкт;
супутня інформація, що стосується проєкту.

ДОДАТКОВО:
для коректного оформлення README можна скористатися онлайн-редакторами:
https://readme.so/editor
https://dillinger.io/"

Трекер витрат є веб-додатком, який дозволяє користувачеві стежити за своїм щоденним витратами коштів.

Додаток має наступні функціональні компоненти:
Головна сторінка,
Сторінка реєстрації,
Сторінка логіну,
Головна сторінка транзакцій,
Сторінка історії транзакцій.

Адаптивність - підтримка мобільних, планшетних і десктопних розмірів екранів (mobile-first підхід).
-мобілки - від 375пікс,
-планшету - від 768пікс,
-десктопу - від 1440пікс.
\*ДОДАТКОВО. Виконати респонсивну верстку для ширини до 375пікс.
Стилізація - реалізована через CSS Modules.
Маршрутизація - App Router Next.js 15. Реалізація публічних та приватних маршрутів (захист сторінок на рівні компонентів).
Стейт-менеджмент - за необхідності використовується Zustand.
Отримання даних - за необхідності використовується React Query.
Індикатори завантаження - відображення лоадера під час усіх активних запитів (на рівні сторінки чи окремих компонентів).
Обробка помилок - повідомлення про помилки запитів та валідації форм (використання toast-повідомлень, статус-повідомлень на формі).
Форми - створення форм через Formik із валідацією на основі Yup.

Ось детальна та зрозуміла інструкція для використання твого стору **UserStore**. Вона написана так, щоб будь-який розробник у команді міг швидко зрозуміти, як отримувати дані та як їх змінювати.

---

# 📘 Інструкція до UserStore (Zustand)

Цей стор відповідає за збереження даних профілю користувача, налаштувань валюти, категорій транзакцій та загальних фінансових підсумків.

## 🛠 Як підключити

Просто імпортуйте хук у ваш React-компонент:

```tsx
import { useUserStore } from "@/store/useUserStore";
```

---

## 🔍 Отримання даних (Selectors)

Щоб уникнути зайвих рендерів, завжди діставайте дані за допомогою селекторів. Компонент буде оновлюватися лише тоді, коли зміниться конкретне вибране значення.

### 1. Дані профілю та валюта

```tsx
const name = useUserStore((state) => state.name);
const currency = useUserStore((state) => state.currency);
```

### 2. Категорії

```tsx
const incomeCategories = useUserStore((state) => state.categories.incomes);
const expenseCategories = useUserStore((state) => state.categories.expenses);
```

### 3. Фінансові підсумки (для Dashboard)

```tsx
const totalIncomes = useUserStore((state) => state.transactionsTotal.incomes);
const totalExpenses = useUserStore((state) => state.transactionsTotal.expenses);
```

---

## ✍️ Зміна даних (Actions)

### 1. Оновлення профілю (`updateProfile`)

Дозволяє змінити ім'я, аватар або валюту. Можна передавати як одне поле, так і декілька одночасно.

```tsx
const updateProfile = useUserStore((state) => state.updateProfile);

// Зміна лише валюти
updateProfile({ currency: "eur" });

// Зміна імені та аватара
updateProfile({ name: "Alex", avatarUrl: "https://new-photo.jpg" });
```

### 2. Оновлення категорій (`setCategories`)

Використовується для повної заміни списку категорій певного типу (після отримання з API або редагування).

```tsx
const setCategories = useUserStore((state) => state.setCategories);

const newExpenses = [
  { _id: "1", categoryName: "Food", type: "expenses" },
  { _id: "2", categoryName: "Rent", type: "expenses" },
];

setCategories("expenses", newExpenses);
```

### 3. Оновлення підсумків (`updateTotals`)

Оновлює суми надходжень та витрат. Можна оновити лише один показник.

```tsx
const updateTotals = useUserStore((state) => state.updateTotals);

// Наприклад, після додавання нової витрати
updateTotals({ expenses: 1250 });
```

---

## 🏗 Структура даних (TypeScript)

Для розробників, які працюють з типами, доступні наступні інтерфейси:

- **`Category`**: `_id`, `categoryName`, `type` ("incomes" | "expenses").
- **`UserState`**: повний об'єкт стану, включаючи екшени.

---

## 🚀 Поради щодо продуктивності

- **Не діставайте весь стор одразу** через `const state = useUserStore()`. Це змусить ваш компонент перерендеритися при будь-якій зміні в сторі (наприклад, зміна суми витрат оновить компонент з аватаркою).
- Використовуйте **Zustand DevTools** у браузері, щоб бачити історію змін стану під назвою `UserStore`.

---
